[storage]
engine = file_system
path = dotfiles
directory = backup

[applications_to_ignore]
ipython
ssh
# Preference domains that break under mackup symlinks on modern macOS (cfprefsd).
# Managed via scripts/hotkeys-export.sh and scripts/hotkeys-restore.sh instead.
rectangle
macosx
